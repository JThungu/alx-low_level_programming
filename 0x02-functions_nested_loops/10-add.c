#include "main.h"

/**
* add - adds two integers and returns the result
* @s: int to be added to t
* @t: int to be added to s
* Return: sum of s and t
*/

int add(int s, int t)
{
	int u;

	u = s + t;
	return (u);
}
