#include "main.h"
/**
 * print_alphabet - Prints all alphabet in lowercase.
 * Return: Always 0
 */



void print_alphabet(void)
{
	char u;

	for (u = 'a'; u <= 'z'; u++)
		_putchar(u);
	_putchar('\n');
}
