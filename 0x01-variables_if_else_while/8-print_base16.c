#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints combination of single digits'
 * Return: Always 0
 */
int main(void)
{
	int k;
	int j;

	for (k = 48; k <= 57; k++)
	{
		putchar(k);
	}
	for (j = 97; j <= 102; j++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
